{% extends 'base.html.twig' %}
{% block body %}

    <h1> profil utilisateur</h1>


        <p>email:  {{ app.user.email }}</p>
    <hr style="background-color: #ffa500">
        <p>pseudo: {{ app.user.username }}</p>


    <a href="{{ path('security_logout') }}"class="btn btn-warning">deconnexion</a>
    <a href="{{ path('site_create') }}"class="btn btn-success">crée une partie </a>


{% endblock %}