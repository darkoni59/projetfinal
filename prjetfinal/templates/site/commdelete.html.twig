<form style="background: #ee2d4d" method="post" action="{{ path('admin_comment', {'id': comment.id}) }}" onsubmit="return confirm('éte vous sûr de vouloir supprimer se commentaire grand maitre');">

    <input type="hidden" name="_method" value="DELETE">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">

    <button class="btn btn-primary">Delete</button>

</form>